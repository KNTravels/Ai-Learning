/* Regenerated on every CI/CD deploy with the real commit SHA and build date
   (see .github/workflows/ai-learning-deploy.yml). This placeholder is what
   ships when running the app locally straight from the repo. */
window.APP_VERSION = {
  version: "dev",
  commit: "local",
  buildDate: "unknown",
};
